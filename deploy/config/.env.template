# Database runs as a Docker container on the VM (service name 'db' resolves in the Compose network)
DATABASE_URL=postgresql+asyncpg://postgres:amphive_db_admin@db:5432/amphive
MQTT_BROKER_HOST=mqtt
MQTT_BROKER_PORT=1883
JWT_SECRET_KEY=change-me-in-production
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
COINS_PER_RUPEE=1.0
COINS_PER_KWH=5.0
