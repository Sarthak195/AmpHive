# Fully-pinned lockfile for the backend runtime image.
#
# This file is what the Docker image actually installs (see backend/Dockerfile).
# backend/requirements.txt remains the human-edited source of truth (loose,
# >= pins with rationale comments); this file is the exact, reproducible set
# resolved from it.
#
# Regenerating (do a CLEAN resolve, not an incremental add):
#   1. Update backend/requirements.txt as usual.
#   2. Resolve into a THROWAWAY venv, so nothing from a dev box leaks in:
#        python -m venv /tmp/lockenv
#        /tmp/lockenv/bin/pip install -r backend/requirements.txt
#   3. /tmp/lockenv/bin/pip freeze  ->  this file (sorted, case-insensitive).
#   4. Install the same set into the repo-root .venv and run
#      `pytest backend/tests`, `ruff check backend/` and `mypy` before
#      committing — a lockfile that has not been exercised is a guess.
#   5. Do not hand-bump versions here; version changes belong in
#      requirements.txt.
#
# Generated: 2026-08-18, clean re-resolve in a throwaway venv on Python 3.13.
# The production image is python:3.11-slim (backend/Dockerfile); every pin
# below was checked to declare Requires-Python compatible with 3.11.
#
# 2026-08-18 (production-readiness audit) — this is the first CLEAN re-resolve
# since 2026-07-21 (the 2026-08-02 google-auth change was an incremental
# hand-add). It closes the four advisories `pip-audit` reported against the
# previous pins:
#   * aiohttp      3.14.1 -> 3.14.3   (PYSEC-2026-3545/3546/3547)
#   * cryptography 49.0.0 -> 50.0.0   (PYSEC-2026-3552)
#   * pyasn1        0.6.3 -> 0.6.4    (PYSEC-2026-3455/3456/3457)
# and moves several long-stale pins that were held only by the lockfile's own
# staleness (bcrypt 4.0.1 -> 5.0.0, fastapi 0.139 -> 0.141, starlette 1.3 ->
# 1.6, uvicorn 0.50 -> 0.52, sqlalchemy 2.0.51 -> 2.0.52, alembic 1.18 ->
# 1.19). Verified: 940 passed / 211 skipped, ruff + mypy clean.
#
# `tapo` was DROPPED from the runtime closure — nothing under backend/ imports
# it (tapo_direct.py was removed 2026-08-02); the bench scripts that do now
# declare it in tools/requirements.txt.
#
# KNOWN, ACCEPTED: `ecdsa==0.19.2` carries PYSEC-2026-1325 (the Minerva timing
# side-channel) with NO fixed version available upstream. It arrives as a
# transitive dependency of python-jose and is only reachable for ES*
# algorithms; AmpHive signs and verifies JWTs with HS256 exclusively
# (backend/services/auth.py ALGORITHM), so the vulnerable code path is never
# entered. Removing it entirely means migrating python-jose -> PyJWT, which is
# an auth-path change and is deliberately NOT bundled into this audit.

aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
attrs==26.1.0
bcrypt==5.0.0
bidict==0.23.1
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
dnspython==2.8.0
ecdsa==0.19.2
email-validator==2.3.0
fastapi==0.141.1
frozenlist==1.8.0
google-auth==2.56.3
greenlet==3.5.5
h11==0.16.0
http_ece==1.2.1
httptools==0.8.0
idna==3.19
Mako==1.4.1
MarkupSafe==3.0.3
multidict==6.7.1
paho-mqtt==2.1.0
propcache==0.5.2
py-vapid==1.9.4
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.3
python-engineio==4.13.5
python-jose==3.5.0
python-socketio==5.16.4
pywebpush==2.4.0
PyYAML==6.0.3
razorpay==2.0.1
requests==2.34.2
rsa==4.9.1
simple-websocket==1.1.0
six==1.17.0
SQLAlchemy==2.0.52
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.3
watchfiles==1.2.0
websockets==17.0.1
wsproto==1.3.2
yarl==1.24.5
