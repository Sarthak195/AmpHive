/**
 * Marketing — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Marketing = () => (
  <main className="page">
    <PageHeader title="AmpHive" sub="This page is being rebuilt." />
  </main>
);

export default Marketing;
