/**
 * AdminUsers — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminUsers = () => <PageHeader title="Users" sub="This page is being rebuilt." />;

export default AdminUsers;
