/**
 * AdminPayouts — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminPayouts = () => <PageHeader title="Payouts" sub="This page is being rebuilt." />;

export default AdminPayouts;
