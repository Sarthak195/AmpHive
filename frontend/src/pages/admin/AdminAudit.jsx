/**
 * AdminAudit — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminAudit = () => <PageHeader title="Audit log" sub="This page is being rebuilt." />;

export default AdminAudit;
