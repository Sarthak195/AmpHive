/**
 * AdminDisputes — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminDisputes = () => <PageHeader title="Disputes" sub="This page is being rebuilt." />;

export default AdminDisputes;
