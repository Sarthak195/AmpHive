/**
 * AdminTenants — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminTenants = () => <PageHeader title="Tenants" sub="This page is being rebuilt." />;

export default AdminTenants;
