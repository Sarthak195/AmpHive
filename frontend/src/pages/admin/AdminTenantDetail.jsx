/**
 * AdminTenantDetail — STUB (Phase E rebuilds this page; AdminLayout provides the shell).
 */

import PageHeader from '../../components/ui/PageHeader';

const AdminTenantDetail = () => <PageHeader title="Tenant" sub="This page is being rebuilt." />;

export default AdminTenantDetail;
