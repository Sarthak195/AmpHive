/**
 * CpoChargers — STUB (Phase D rebuilds this page inside CpoLayout).
 */

import useTheme from '../../hooks/useTheme';
import PageHeader from '../../components/ui/PageHeader';

const CpoChargers = () => {
  useTheme('volt');
  return (
    <main className="console-main">
      <PageHeader title="Chargers" sub="This page is being rebuilt." />
    </main>
  );
};

export default CpoChargers;
