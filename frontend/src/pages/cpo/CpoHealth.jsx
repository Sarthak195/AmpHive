/**
 * CpoHealth — STUB (Phase D rebuilds this page inside CpoLayout).
 */

import useTheme from '../../hooks/useTheme';
import PageHeader from '../../components/ui/PageHeader';

const CpoHealth = () => {
  useTheme('volt');
  return (
    <main className="console-main">
      <PageHeader title="Health" sub="This page is being rebuilt." />
    </main>
  );
};

export default CpoHealth;
