/**
 * Signup — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Signup = () => (
  <main className="page">
    <PageHeader title="Create account" sub="This page is being rebuilt." />
  </main>
);

export default Signup;
