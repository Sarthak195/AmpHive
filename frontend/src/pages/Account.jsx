/**
 * Account — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Account = () => (
  <main className="page">
    <PageHeader title="Account" sub="This page is being rebuilt." />
  </main>
);

export default Account;
