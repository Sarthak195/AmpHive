/**
 * MapPage — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const MapPage = () => (
  <main className="page">
    <PageHeader title="Map" sub="This page is being rebuilt." />
  </main>
);

export default MapPage;
