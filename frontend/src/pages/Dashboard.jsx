/**
 * Dashboard — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Dashboard = () => (
  <main className="page">
    <PageHeader title="Home" sub="This page is being rebuilt." />
  </main>
);

export default Dashboard;
