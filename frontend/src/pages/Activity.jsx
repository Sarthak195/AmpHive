/**
 * Activity — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Activity = () => (
  <main className="page">
    <PageHeader title="Activity" sub="This page is being rebuilt." />
  </main>
);

export default Activity;
