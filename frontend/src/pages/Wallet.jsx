/**
 * Wallet — STUB (Phase C rebuilds this page).
 */

import PageHeader from '../components/ui/PageHeader';

const Wallet = () => (
  <main className="page">
    <PageHeader title="Wallet" sub="This page is being rebuilt." />
  </main>
);

export default Wallet;
